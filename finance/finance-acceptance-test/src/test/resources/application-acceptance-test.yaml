app:
  base-url: http://localhost:8080

category:
  url: ${app.base-url}

spring:
  application:
    name: finance-acceptance-test
  
  datasource:
    url: jdbc:postgresql://localhost:5432/pfm_finance
    username: pfm
    password: pfm
    driver-class-name: org.postgresql.Driver

feign:
  client:
    config:
      default:
        connectTimeout: 10000
        readTimeout: 10000
        loggerLevel: FULL

